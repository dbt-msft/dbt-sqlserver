With `dbt_sqlserver_use_dbt_transactions` on, `dbt run-operation` no longer silently rolls back a macro's `statement()` writes: the adapter commits when the operation finishes cleanly, as a stopgap for [dbt-labs/dbt#16434](https://github.com/dbt-labs/dbt/issues/16434). A failed macro still rolls back.
