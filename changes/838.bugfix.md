An unchanged `view` model now runs `sp_refreshview` only when its cached column metadata differs from what its body resolves to, so `sys.objects.modify_date` again separates a skipped run from a rebuild.
