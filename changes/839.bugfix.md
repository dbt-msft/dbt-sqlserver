Fix concurrent `CREATE SCHEMA` failing with `Msg 2714` (`There is already an object named '<schema>'`) under `threads > 1` or parallel runs. Schema creation in `create_schema`, `create_schema_with_authorization`, data tests and unit tests is now serialized behind an `sp_getapplock`, and schema names containing an apostrophe no longer break the statement.
